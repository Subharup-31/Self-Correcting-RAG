# ============================================================
# Ultimate Self-Correcting RAG — Requirements
# ============================================================

# --- Core LangChain / LangGraph ---
langchain>=0.3.0
langgraph>=0.2.0
langchain-core>=0.3.0
langchain-community>=0.3.0

# --- LLM Providers ---
langchain-google-genai>=2.0.0   # Gemini 1.5 Flash (default)
langchain-openai>=0.2.0         # Optional: OpenAI GPT-4o-mini

# --- Vector Store ---
langchain-chroma>=0.1.4
chromadb>=0.5.0

# --- Embeddings & Reranking ---
sentence-transformers>=3.0.0    # BAAI/bge-m3 + cross-encoder/ms-marco-MiniLM

# --- Retrieval ---
rank_bm25>=0.2.2                # BM25 keyword retrieval

# --- Web Search ---
langchain-tavily>=0.1.5
tavily-python>=0.3.0

# --- Document Ingestion & OCR ---
unstructured[all-docs]>=0.15.0  # Universal document loader
pytesseract>=0.3.10             # OCR for scanned images
pdf2image>=1.17.0               # PDF -> image conversion
pymupdf>=1.24.0                 # Digital PDF text extraction
pillow>=10.0.0                  # Image processing

# --- Evaluation ---
ragas>=0.1.0                    # RAG evaluation metrics
datasets>=2.0.0                 # HuggingFace datasets

# --- API Server ---
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
sse-starlette>=2.0.0            # Server-sent events for streaming

# --- Utilities ---
pydantic>=2.0.0
python-dotenv>=1.0.0
aiofiles>=24.0.0
httpx>=0.27.0
tenacity>=8.0.0                 # Retry logic
loguru>=0.7.0                   # Structured logging

# --- Cloud Vector Store & HuggingFace ---
langchain-qdrant>=1.1.0
qdrant-client>=1.18.0
langchain-huggingface>=1.2.0

